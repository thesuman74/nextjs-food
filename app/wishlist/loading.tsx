import { CardSkeleton, WishlistSkeletion } from "@/components/ui/Skeletions";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <WishlistSkeletion />;
}
